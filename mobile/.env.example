# Copy to `.env.local` for local overrides (not committed).

EXPO_PUBLIC_API_BASE_URL=https://api.gishathfetch.com
EXPO_PUBLIC_SITE_BASE_URL=https://gishathfetch.com

# Set after linking the project with `eas init` / `eas build:configure`
# EAS_PROJECT_ID=
